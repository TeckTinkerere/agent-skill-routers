# Frontend examples

## Example 1: SaaS landing

**Input:** "Build a landing page for a developer API product"

**Expected:** Design brief with technical audience, code-adjacent signature (live snippet or terminal motif), not generic startup gradient.

## Example 2: Dashboard

**Input:** "Add a settings page to our app"

**Expected:** SymDex existing settings/layout patterns first; match design system; no full redesign of unrelated pages.

## Example 3: "Make it pop"

**Input:** "Make the hero more exciting"

**Expected:** Revise signature element in brief; if motion needed → hand off `routr-motion` after structure exists.
