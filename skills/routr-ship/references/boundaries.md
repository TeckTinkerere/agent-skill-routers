# routr-ship boundaries

| Situation | Use instead |
|-----------|-------------|
| Don't know why it breaks | `routr-debug` |
| Need tests written first | `routr-test` |
| PR review feedback only | `routr-review` |
| Production deploy | `routr-deploy` |

Ship = verify + commit/PR. Deploy is separate.
