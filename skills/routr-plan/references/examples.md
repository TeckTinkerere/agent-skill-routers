# Planning examples

## Example 1: "Add auth"

**Expected:** Brainstorm session types (magic link vs OAuth vs passkeys); no code until approach chosen.

## Example 2: "Grill my startup idea"

**Expected:** `grill-me` adversarial questions; revised plan or explicit open questions.

## Example 3: Small bugfix

**Expected:** Do NOT activate routr-plan — use `routr-debug` → `routr-ship`.
