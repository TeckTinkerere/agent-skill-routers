# routr-plan boundaries

| Situation | Use instead |
|-----------|-------------|
| How does existing code work? | `routr-explore` |
| Known fix, ship it | `routr-ship` |
| Multi-agent harness design | `routr-agents` |
| Write tests for feature | `routr-test` |

Planning ends when PRD/approach approved; execution uses domain routers.
